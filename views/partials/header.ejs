<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>PinkGetters</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <!-- 自定义CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- 页面头部 -->
    <header>
        <!-- 导航栏 -->
        <nav class="navbar navbar-expand-lg fixed-top" role="navigation" aria-label="主导航">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-heart-fill"></i> PinkGetters
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto align-items-lg-center">
                    <li class="nav-item">
                        <a class="nav-link" href="/">首页</a>
                    </li>
                    <li class="nav-item dropdown nav-categories">
                        <a class="nav-link dropdown-toggle" href="/categories" id="navCategoriesLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            商品分类
                        </a>
                        <div class="dropdown-menu shadow" aria-labelledby="navCategoriesLink" id="nav-category-dropdown">
                            <div class="py-2 px-3 text-muted small">加载中...</div>
                        </div>
                    </li>
                </ul>

                <!-- 搜索框 -->
                <form class="d-flex nav-search ms-lg-3 me-lg-3 my-3 my-lg-0" role="search" id="nav-search-form" action="/search" method="get">
                    <div class="input-group nav-search-group">
                        <select class="form-select nav-search-select search-type-narrow" name="type" aria-label="搜索类型">
                            <option value="product" <%= typeof searchType === 'undefined' || searchType === 'product' ? 'selected' : '' %>>商品</option>
                            <option value="shop" <%= typeof searchType !== 'undefined' && searchType === 'shop' ? 'selected' : '' %>>店铺</option>
                        </select>
                        <input class="form-control" type="search" name="q" placeholder="搜索商品 / 店铺" aria-label="搜索" id="nav-search-input" value="<%= typeof searchKeyword !== 'undefined' ? searchKeyword : '' %>">
                        <button class="btn btn-light" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>
                

                <ul class="navbar-nav ms-lg-2">
                    <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
                        <li class="nav-item">
                            <a class="nav-link position-relative" href="/cart">
                                <i class="bi bi-cart"></i> 购物车
                                <span class="cart-badge position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="display: none;">
                                    0
                                </span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/user/home">个人中心</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/logout">退出</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/login">登录</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/register">注册</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    </header>
    

